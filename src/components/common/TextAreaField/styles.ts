import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    padding: 6,
    fontSize: 16,
    borderColor: 'black',
    borderStyle: 'solid',
    borderWidth: 1,
    height: 100,
    verticalAlign: 'top',
  },
});
